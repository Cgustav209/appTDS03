## Istalando a dependencia do TypeScript
npx expo install typescript @types/react
## Renomeie o App.Js para App.tsx 'extenção do TS'

## Instalando dependencia de Camera
npx expo install expo-camera

## Acesso a `Pasta de Imagens
npx expo install expo-image-picker